		<div id="smplslidetn" class="smplslidetn_container {{ tnmiddle }}" data-id="{{ id }}">
			<div id="smplslidetndiv{{ id }}" class="smplslidetn_image_div">
				<img id="smplslidetn{{ id }}" src="{{ linktn }}" title="{{ titletn }}"
					alt="{{ slidetnsubtitle }}" class="smplslidetnimg"
					data-id="{{ id }}" data-link="{{ linktn }}" />
			</div>
		</div>
