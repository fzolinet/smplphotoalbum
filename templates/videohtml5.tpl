<div id="div{{id}}" class="smpl_image_div" {{smpl_simplelist}}>
  <video id="{{id}}" controls  {{attrib}} style="{{sty}}">
    <source src="{{videosrc}}" type="{{mime}}" >
    Your browser does not support the video tag.
  </video>
</div>

