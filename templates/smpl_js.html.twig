;
class Words {
	Delete   = "{{ Delete }}";
	Rename   = "{{ Rename }}";
	ImageEdit= "{{ Image Edit }}";
	Undo     = "{{ Undo }}";
	Redo     = "{{ Redo }}";
	Subtitle = "{{ Subtitle }}";
	Del_not  = "{{ Can not delete }}";
	Confirm  = "{{ OK }}";
	Cancel   = "{{ Cancel }}";
	OpenInNewTab = "{{ Open in new tab }}";
  Close    = "{{ Close }}";
}

class SmplPhotoalbum {
  ajax = "{{ base_path }}smplphotoalbum";        
  autohistogram = false;
  bheight = 0;
  bkp = 0;
  bwidth = 0;
  ctx = "";
  editsaved = false;
  exif = ""; 
  exif_info_show = false;
  filesize = 0;
  graphicdrv = "{{ graphicdrv }}";
  height = 0;
  i = 1; 
  id = {{ id }}; // actual id
  idx = 0;       // actual index of edited file  
  imagick = "{{ imagickversion }}";
  imgeditform = {{ imgeditform }};
  imgeditsaved = false;  //saved modified file
  maxsize = "{{ maxsize }}";  //max size of uploaded file
  modified = ""; 
  modulepath = "{{ modulepath }}";  
  name = "";     // Original filename
  oheight = 0;
  owidth = 0;
  pars = "";
  rgbcolor = "#808080"; 
  signurl = "";  // sign file url  
  tempurl = "";  // Original temp path
  timer = "0%";  // Timer variable
  translateX = 0.0;
  translateY = 0.0;  
  test = false;
  testmsg = "";
  url = "";
  view = "";
  width = 0;
  wm = true;
  wmpath = "{{ wmpath }}";
  words = new Words();

  application= "{{ extapplication }}";
  audio      = "{{ extaudio }}";
  audiohtml5 = "{{ extaudiohtml5 }}";
  compressed = "{{ extcompressed }}";
  document   = "{{ extdocument }}";
  images     = "{{ extimages }}";
  other      = "{{ extother }}";
  video      = "{{ extvideo }}";
  videohtml5 = "{{ extvideohtml5 }}";
  extensions = "{{ extensions }}";
 }
var smpl = new SmplPhotoalbum();
var smplstat = false;
;